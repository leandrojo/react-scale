{
  "name": "wide-workspace",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "lerna run start --parallel --scope @wide/wide-*"
  },
  "workspaces": {
    "nohoist": [
      "react-scripts",
      "react-scripts/**"
    ],
    "packages": [
      "packages/*"
    ]
  }
}
